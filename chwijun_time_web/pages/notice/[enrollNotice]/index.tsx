import React from 'react';
import { Template, EnrollNotice } from 'components';

const EnrollNoticePage:React.FC = () => {
    return(
        <Template>
            <EnrollNotice />
        </Template>
    )
}

export default EnrollNoticePage;